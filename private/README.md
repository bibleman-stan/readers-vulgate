# private/ — gitignored working folder

This repo is **public**. Everything in `private/` is gitignored (except this README)
and is **never published**. Use it for license-restricted notes, draft/working
material, or anything that must not appear in the public repo.

The actual substrate **data** lives in the `~/repos/biblical-corpora/` container
(also unpublished) — `private/` here is for repo-local working material only.
